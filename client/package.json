{
	"name": "jetly-react",
	"version": "0.1.0",
	"private": true,
	"dependencies": {
		"react": "^16.6.3",
		"react-app-rewire-hot-loader": "^1.0.3",
		"react-app-rewired": "^1.6.2",
		"react-dom": "^16.6.3",
		"react-hot-loader": "^4.3.12",
		"react-scripts": "2.1.1"
	},
	"scripts": {
		"start": "react-scripts start",
		"build": "react-scripts build",
		"test": "react-scripts test",
		"eject": "react-scripts eject",
		"build-css": "npx node-sass-chokidar src/ -o src/",
		"watch-css": "npm run build-css && npx node-sass-chokidar src/ -o src/ --watch --recursive"
	},
	"proxy": "http://localhost:5000/",
	"eslintConfig": {
		"extends": "react-app"
	},
	"browserslist": [
		">0.2%",
		"not dead",
		"not ie <= 11",
		"not op_mini all"
	],
	"devDependencies": {
		"react-app-rewire-scss": "^1.0.2"
	}
}
